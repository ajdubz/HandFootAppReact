rules_version='2'

service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // Keep Firestore closed until app-specific ownership rules are added.
      allow read, write: if false;
    }
  }
}
